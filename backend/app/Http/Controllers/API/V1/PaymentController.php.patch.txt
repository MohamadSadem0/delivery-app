// In webhook():
// $verifier = new \App\Domain\Payment\Services\PaymentSignatureVerifier();
// if (!$verifier->verify($payload, $provider, $request->header('X-Signature'))) {
//     return response()->json(['message'=>'Invalid signature'],403);
// }
