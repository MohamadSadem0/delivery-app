// src/constants/config.ts
export const DEFAULT_TIMEOUT_MS = 15000;

export const STORAGE_KEYS = {
  token: 'auth.token',
  refreshToken: 'auth.refreshToken',
} as const;

export const APP_SCHEME = 'deliveryapp';

